[project]
name = "rdc-pipeline-sync-batch"
version = "0.1.0"
description = "SyncBatch pipeline Lambda (FindByPath + SearchDrinks + SyncProduct unificados)"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.13",
    "pillow>=11",
]

[tool.uv]
package = false
