[project]
name = "rdc-pipeline-sync-product"
version = "0.1.0"
description = "SyncProduct pipeline Lambda"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.13",
    "pillow>=11",
]

[tool.uv]
package = false
