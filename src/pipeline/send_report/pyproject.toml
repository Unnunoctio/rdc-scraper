[project]
name = "rdc-pipeline-send-report"
version = "0.1.0"
description = "SendReport pipeline Lambda"
requires-python = ">=3.14"
dependencies = [
    "openpyxl>=3.1",
    "resend>=2.0",
    "tzdata>=2025.1",
]

[tool.uv]
package = false
