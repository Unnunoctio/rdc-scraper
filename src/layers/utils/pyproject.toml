[project]
name = "rdc-utils"
version = "0.1.0"
description = "Utils layer for RDC Scraper (fetchers, standard_format, naming, mappings)"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.13",
    "beautifulsoup4>=4.14",
]

[tool.uv]
package = false
