export enum TimeUnit {
  SEC = 1000,
  MIN = 60000,
  HOUR = 3600000
}

export enum SpiderName {
  JUMBO = 'Jumbo',
  LIDER = 'Lider',
  SANTA = 'Santa Isabel',
}

export enum EmailSend {
  SEND = 'SEND',
  SKIP = 'SKIP'
}
