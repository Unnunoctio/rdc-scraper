# Prototipo archivado (referencia local, no se versiona)
.deprecated/

# SAM / build
.aws-sam/
samconfig.toml

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.ruff_cache/

# uv
# (uv.lock SÍ se versiona)

# OS / editor
.DS_Store
*.swp
